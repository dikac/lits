import Empty from './abstract-compound';
import Validatable from "../validatable/validatable";
//
// export default abstract class <Value, Return extends Validatable> extends Empty<Value, Return> {
//
//     validate(booleans: Iterable<boolean>) : Return {
//
//         let bool : boolean|null = null;
//
//         for(bool of booleans) {
//
//             if(bool.valueOf()) {
//
//                 return true;
//             }
//         }
//
//         return bool ? false : this.empty;
//     }
// }

